FROM sagebionetworks/synapsepythonclient:v2.5.1

# This Dockerfile is used as a placeholder
# dockstore tools are required to have a Dockerfile
# associated with them.  This Dockerfile is never used
# to build a docker image.  Git submodules are also
# not recognized by dockstore.

# The original Dockerfile can be found here
# https://github.com/Sage-Bionetworks/synapsePythonClient